services:
  - type: static
    name: frontend-fms
    env: static
    repo: ""
    branch: dev
    buildCommand: "npm ci && npm run build"
    publishPath: "dist"
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
